# 弱LLM単発委譲プロンプト テンプレート
# 発注側(上位モデル)がタスクごとに埋めて claude-local -p / tmux に投げる。
# standard×明示タスクは 1,2,5,6 だけでよい。bespoke は 4 を必ず埋める。

【目的】<何を作るか。ファイル名まで指定。1文>

【仕様】
(1) 入力: <形式・ソース>
(2) 出力: <項目を全列挙。書かないと弱LLMが自己判断で埋める>
(3) 制約: <禁止事項(例: eval禁止/外部依存禁止)、計算量、エラー処理方針>

【決定権】内部実装・変数名は自由。ただし <関数名/戻り値/エラー型など厳守すべきもの> は指定通り厳守。

【急所】  # bespoke タスクは必須。standard は省略可
- <推測で外しうる境界条件を名指し>
- 入出力例(複数): `<入力>` → `<期待出力>` を並べ、境界を具体例で固定する
  例) 接触区間の扱い、境界値が <= か < か、空入力、未ソート入力 など

【自己検証】test_<名前>.py に pytest を最低 <N> ケース書き、
python3 -m pytest test_<名前>.py -q を実行して全通過を確認する。
必ず含めるケース: <発注側が通ってほしい観点。ただし隠しテストの答えはリークさせない>

【出力契約】全テスト通過したら「DONE」とだけ報告する。解説は不要。
