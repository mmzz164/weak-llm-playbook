# FINDINGS — 弱LLM委譲の実験記録(2026-07-08〜11)

> 個人環境(ローカルvLLM: Qwen3.6-27B-NVFP4 / Phi-3.5-mini / GLM-5.2 等)での実験ログをほぼそのまま公開している。
> パスやサーバー構成は環境固有のもの。結論・数値・手法が本体。
> ツールの使い方は [USAGE.ja.md](USAGE.ja.md) / [USAGE.md](USAGE.md) を参照。


# 弱LLM向け 委譲タスク指示テンプレート

対象: ローカル中級モデル(Qwen3.6-27B 等)に claude-local 経由でコーディングを委譲するとき。
根拠: 2026-07-08 の実測(weak-llm-lab)。曖昧指示は詳細指示に対し 入力1.5x・出力2.3x・往復1.3x のコスト、かつ品質は自己判断で発散。

## 原則(なぜ効くか)
弱LLMは「明示スペックの実装」は上位モデル並みにこなすが、「曖昧さの補完」でウロウロして発散する。
→ 考える部分(何を・どう検証するか)は発注側(上位モデル/人間)が固め、弱LLMには実装だけをやらせる。

## テンプレート(5ブロック)

### 1. 目的と成果物(1文)
「何を作るか」を1文で。ファイル名まで指定する。
例: 「calc.py に evaluate(expr:str)->float を実装する」

### 2. 仕様(箇条書き・番号付き)
入力形式・出力項目・制約を漏れなく列挙。弱LLMは書いていないことは自己判断で埋めるので、
「埋めてほしくない箇所」は全て書く。
- 入力: <形式・ソース>
- 出力: <項目を全列挙>
- 制約: <禁止事項(例: eval禁止、外部依存禁止)、エラー処理の方針>

### 3. 決定権の範囲(明示)
「自由に決めてよい / 必ずこの通り」を分ける。これがないと勝手に仕様を確定させる。
例: 「変数名・内部構造は自由。ただし関数シグネチャとエラー型は指定通り」

### 4. 自己検証手順(成功条件を埋め込む)
成功を機械的に確認できる手順を指示に入れる。弱LLMに自分でテストを書かせ・実行させる。
例: 「test_X.py に正常系N・異常系Mのpytestを書き、python3 -m pytest で全通過を確認」

### 5. 出力契約(余計な生成を抑制)
「完了したら DONE とだけ報告」等。長い自己解説を抑え出力トークンを節約。

## アンチパターン(実測で確認した失敗モード)
- 探索迷子: 曖昧指示だと ls から延々さまよう → 目的とファイル名を最初に固定する
- 暗黙の自己判断: 決定権が不明だと検証不能な成果物になる → ブロック3で範囲を切る
- 過剰実装: 指定がないと余計な機能・行数を盛る → 仕様を「これだけ」と閉じる

## 発注側(上位モデル)の責務
- タスクを渡す前にスペックを完成させる(スペック作成は数千tokで安い)
- 戻ってきた成果物は独立に検証する(弱LLMの自己申告を信じない)
- 探索が必要な曖昧タスクは、まず自分でスペック化してから委譲する

---
## 実測ログ(2026-07-08, Qwen3.6-27B-NVFP4 / claude-local)

| 実験 | タスク | 指示 | 入力tok | 出力tok | 品質 |
|---|---|---|---|---|---|
| 1 | 数式パーサ | 詳細+検証 | 96,558 | 1,126 | 完璧(pytest8+隠し5全通過) |
| 2A | ログ集計 | 曖昧1行 | 187,538 | 5,124 | 動くが仕様全て自己判断・323行 |
| 2B | ログ集計 | 詳細+契約 | 126,019 | 2,262 | 仕様6項目充足 |
| 4C | LRU | 素の1行 | 93,676 | 779 | 隠し7/7通過(有名問題は素でも可) |
| 4D | LRU | テンプレ準拠 | 96,102 | 954 | 隠し7/7 + 自作テスト7件 |

### 結論
- 曖昧さのコストは非有名タスクで顕在化(実験2: 曖昧は詳細の入力1.5x/出力2.3x、品質は発散)。
- 有名・定型タスク(LRU等)はモデルの素振り済みで、素の指示でも品質が出る(実験4: C/D差は小)。
  → テンプレの価値は「モデルが迷う非定型タスク」で最大。定型タスクには過剰。
- テンプレの副次効果: 自己検証手順を入れると弱LLMが自分でテストを書き実行する(4Dで7件自作)。
  発注側の検証コストを下げる。
### 運用指針
タスクを「モデルにとって定型か/非定型か」で振り分ける:
- 定型(有名アルゴリズム、CRUD、定番変換) → 素の指示でよい。テンプレは出力契約だけ足す。
- 非定型(独自仕様、プロジェクト固有ロジック、曖昧な要件) → フルテンプレ必須。

---
## 深掘り: subtle correctness の境界(2026-07-08 追加, 実験5-7)

| 実験 | タスク | 罠の種類 | 罠の明示 | 入力tok | 隠しテスト |
|---|---|---|---|---|---|
| 5E | semver比較 | standard・prerelease優先順位 | なし(semver.org準拠のみ) | 214k | 罠7/7 全通過 |
| 6G | 区間マージ(gap許容) | bespoke・自然な既定解釈 | なし | 72k | 罠7/7 全通過 |
| 7H1 | 区間マージ(接触=非統合) | bespoke・反直感(標準の逆) | 一度・一例で明示 | 119k | 罠5/5 全通過 |

### 深掘りの結論
- 当初仮説「bespokeのsubtleは罠を明示しないと失敗」は棄却。真の軸は standard/bespoke でも
  subtleさでもなく「**明示されているか**」。
- 弱LLMは書いた規則には従う。反直感ルール(接触は統合しない=標準マージの逆)ですら、
  一度・一例で書けば訓練データの標準挙動に引きずられず正答した。
- 失敗するのは「書いていないこと」= 曖昧(実験2)と 非既定ルールの省略。後者は、発注側が
  逸脱点を言い落とすとモデルが既定(標準挙動)で「正しく」実装し意図とズレる形で顕在化する。
- よって発注側の責務は「難しさの見極め」より「逸脱点の言い落としゼロ化」。standard×既定通りは
  薄い指示でよく、過剰スペックは無駄。

---
## 既定プローブ・ハーネス(2026-07-08, 粒度問題への回答)
道具: `default_probe.py`(スキル weak-llm-playbook/scripts に同梱)。
20判断点を各N回投げ、生成コードを実行して既定と安定性を測る。JSON保存+ `--diff` 対応。

### Qwen3.6-27B 既定プロファイル(20点, N=5)
揺れる=必ず明示: avg_empty(例外/0), fib_index(定義ゆらぎ)。
安定だが意図とズレやすい: nth_index=0-indexed / sort_case=ASCII順 / range_incl=終端含む /
missing_key=KeyError / div_zero=例外 / none_input=例外。
安定で直感的: interval_touch=接触も統合 / round_half=2.5→3 / dedup=順序保持 / split_multi=空白畳む /
empty_split=空リスト / flatten=深く平坦化 / reverse=昇順 / title_case=各語頭 / neg_modulo=python式。

### 粒度問題の最終回答
明示すべき判断点 = 「安定性が低い ∪ 既定が意図と食い違う」だけ。それ以外は書かない(過剰は無駄)。
①はプローブが自動検出、②は既定の中身を提示して発注側が照合する。

### モデル別検知
`--diff A.json B.json` でモデル間の既定差を抽出=「モデルを替えたら書き換える点」。
合成モデルで検証済み(反転3点を★、揺れ1点を△で正しく抽出)。
※実モデル比較(step3p7/minimax/gemma)は保留: 各バックエンドの conda env が消失
  (vllm / vllm-step)、または両GPU必須でQwenと競合、GLMは手起動要。env修復は別タスク。
  思考モードは1リクエスト2000tok級で20点×5回が非現実的+同一重みのため真の別モデルでない→不採用。

---
## 実モデル間 diff 成立(2026-07-08, タスク完遂)
判断点を32点に拡張し、Phi-3.5-mini(3.8B)を m3vllm環境でGPU1に配信(Qwen無傷)して実測。
起動の罠: FlashInferサンプリングJITがnvcc未検出で落ちる→ `VLLM_USE_FLASHINFER_SAMPLER=0` +
`--enforce-eager` で回避(スクリプト /var/hf/dl_probe_models/serve_phi.sh)。

### Qwen3.6-27B vs Phi-3.5-mini の既定差(9/32点)
★既定が違う: dedup_order / range_incl / split_multi / missing_key / date_fmt / max_empty / dup_count
  - missing_key: Qwen=KeyError vs Phi=None返す ← 同じ「辞書から値を取る」でも真逆
  - range_incl: Qwen=終端含む vs Phi=終端含まず
  - date_fmt: Qwen=ISO(0埋め) vs Phi=スラッシュ区切り
△不安定(弱いPhiで顕著): dedup/split_multiはPhiがNameError/ModuleNotFoundErrorを出す(安定0.4)
  = 弱いモデルほど「そもそも正しく実装できない=明示しても危うい」点が増える。

### 結論(モデル別検知の実証)
- 同じ意図でも、明示すべき点はモデルで入れ替わる(9/32が差分)。prof をモデルごとに1回取り、
  `--diff` すれば「乗り換え時に書き換える点」が機械的に出る。
- 弱いモデルは「既定が違う」だけでなく「安定して実装できない」点が増える。後者は明示だけでは
  救えず、タスク自体を避けるか粒度を上げる(plan-and-delegateのLv昇降)判断材料になる。

---
## 改善2件(2026-07-08)

### (a) プローブの予測妥当性を検証: 直叩き ≒ エージェント委譲
懸念: プローブはvLLM直叩きで既定を測るが、実委譲はclaude-local(Claude Codeのsysプロンプト+
エージェントループ)経由。両者がズレたらプローブは的外れを測っている。
検証: Qwenの直叩き既定が明確な3点(missing_key=KeyError/range_incl=終端含む/nth_index=0-indexed)
を、仕様を伏せてclaude-localに実装させ挙動を照合 → **3/3 的中**。
結論: 安いvLLM直叩きプローブは高価なエージェント委譲の挙動を予測できる=代理指標として妥当。

### (b) 適応的リサンプリング(解像度向上)
N=5では安定性が0.6/0.8/1.0の粗い値しか出ず境界判定が甘い。安定性が曖昧帯(0.5〜0.85)の点だけ
追加サンプルを取り最大N=15まで精密化。無駄打ちなし(安定点はN=5のまま)。
効果(Qwen): avg_empty 0.6→0.53(N15, 例外8/0返す7=真の五分五分と確定) /
max_empty 0.8→0.86(N7, 実は安定側と判明) / dup_count 0.53(真に不安定+分類器の弱さも露呈)。
= 「必ず明示すべき真に揺れる点」と「実は安定な点」を切り分けられるようになった。

---
## スキルの操作者モデル依存性の検証(2026-07-09)
問い: weak-llm-playbook を実行する側(オーケストレータ)がFable 5でなくOpus/Haikuでも機能するか。
方法: 同一スキル+Qwen既定プロファイルを Opus と Haiku のサブエージェントに読ませ、意図がQwen既定と
逆になる2点(欠損キー→None[既定KeyError]、範囲→半開[既定終端含む])+一致1点(不足時全件[既定一致])
を仕込んだ委譲タスクのスペックを書かせて採点。

結果:
- Opus: 満点+α。2ズレをピン留め、一致点はテストのみ、さらに未設計の曖昧さ(「上位n件」=ソートか
  先頭nか)を自力検知+弁別例で固定、囮点(nth_index/none_input)を関連性で正しく除外。発注側の水準。
- Haiku(下限): 設計3点の②照合は全正解(プロファイル正確引用)。傷2つ=一致点をスペック本文に冗長記載
  (分析では不要と正答)、未設計の曖昧さは見逃し。核心は外さず。

結論(設計原理): スキルが判断を「機械的手順+データ(プロファイル)」に外部化しているため、②照合
(既定vs意図の突き合わせ=「2値の比較」)は Haiku でも遂行できる=操作者モデルへの移植性が高い。
能力が効くのは「プロファイルに無い新規の曖昧さの検知」「判断点の関連性推論」「内部整合性」——
Opusは処理、Haikuは一部取りこぼす。実務含意: 弱い操作者ほどプロファイル(プローブ点)を厚くして
操作者自身の推論に委ねる部分を減らす。強い操作者はプロファイルを叩き台に新規曖昧さも拾う。

---
## 改善3: タスク駆動スペック穴検出器 spec_holes.py(2026-07-09)
動機: 操作者検証で残った唯一の能力依存=「固定バッテリーに無いタスク固有の曖昧さの検知」
(Opusは推論で捕捉、Haikuは見逃した)を機械化する。

方式(disagreement probing): ドラフト仕様をワーカーにK回実装させ、発注側が用意した
プローブ入力で全実装を実行。挙動が割れた入力=仕様の穴として自動報告。3シグナル:
[発散]=必ず明示 / [合意]=意図と照合のみ / 実装不能率=委譲可否。

実証(GLM-5.2をワーカー、top_nドラフト仕様):
- 「上位n件」のソート/先頭曖昧性を機械検出([3,2]×3 vs [1,2]×1) = Haikuが見逃した穴
- 想定外の第2の穴「全件返す時の順序」([2,1] vs [1,2])まで検出
- 合意3件(空/n=0/単一)は照合リストに分離
開発中の知見:
- ワーカーにエッジ入力を提案させるのは脆い(GLM-5.2は`[`反復で崩壊)→発注側供給を正に
- 全入力で例外の壊れ実装は発散分析から除外(ノイズ源)
- 副産物: GLM-5.2-VQ(:8001)はコード生成の実装不能率17-55%と不安定(Qwenよりかなり悪い)。
  spec_holes はモデル健全性チェックとしても機能する
※この日、:8000のQwenは停止され :8001 に glm-5.2 が起動されていた(ユーザー側の切替)。

---
## 改善4: 測定系の汎用エンドポイント対応(2026-07-09)
default_probe.py / spec_holes.py を共通クライアント(llm_client.py)化し、任意のエンドポイントを
測定対象にできるようにした。
- --api openai (既定): /v1/chat/completions — vLLM/llama.cpp/ollama/OpenAI等。Bearer認証対応
- --api anthropic: /v1/messages — Anthropic API本家/claude-code-router等。x-api-key+Bearer両送
- 認証: --key または PROBE_API_KEY/ANTHROPIC_API_KEY/OPENAI_API_KEY
- モデル差異の自動吸収: chat_template_kwargs非対応→外して再試行、temperature拒否
  (Fable5/Opus4.7+)→400検知で外して再試行
ライブ検証: openai=GLM直(:8001)✓ / anthropic=ccr(:3456)経由GLM ✓(このためccrに
local-glmプロバイダを追加・再起動 — 副次効果としてclaude-localからGLMが使用可能になった:
/model local-glm,glm-5.2)。config backupは config.json.bak.YYYYMMDD-*
これで「本家AnthropicのHaikuをプローブしてローカルモデルとdiff」等、商用APIとの既定比較も可能。
追補(同日): --only フラグ(判断点の限定実行、保存は *_partial.json で本体を汚さない)と、
レポートに「実装不能」カテゴリ(多数派がエラー=明示しても救えない)を追加。
E2E検証: ccr(anthropic形式)→GLMでプローブ完走。副産物: GLMのmissing_keyは適応リサンプリングで
None返す6 vs KeyError8 と判明(=実は不揺れでなく不安定。Qwenは KeyError で安定1.0 と対照的)。

---
## 改善5: model引数の自動検出(2026-07-09)
動機: model は argparse 上は省略可に見えて、実際は個人環境の既定値 Qwen3.6-27B-NVFP4 に
落ちる作りだった(公開ツールとして罠 — vLLM は model 名不一致で404を返す)。
- llm_client.py に detect_model(base, key): GET /v1/models から配信中モデル一覧を取得
- default_probe.py / spec_holes.py: 先頭(spec_holesは第3)引数がURLなら model 省略とみなす
  argv繰り上げ + "" プレースホルダ対応。--api anthropic は明示必須(argparseエラーで案内)
- 検証: モックOpenAI互換サーバー(:18999)で3経路(URL先頭省略/""/明示)+anthropicガードを確認
- README(en/ja)/SKILL.md×2 更新、3コピー同期、repo push 済み(2ddab01)
- 教訓再確認: pkill -f は自分のシェルのコマンド行に自己マッチして自滅する(exit 144)

---
## 改善6: コーディング以外への拡張 — ioバッテリー+抽出モード(2026-07-10)
背景: GPU1が空いたのでQwen3.6をGPU1で再配信(`QWEN_GPUS=1 bash /var/hf/start_qwen36_nvfp4.sh`,
:8000, ログ=qwen36-nvfp4.gpu1.vllm.log)し、コーディング以外を測定対象に拡張。
(a) `default_probe --domain io`: 18判断点(構造化出力10 / 抽出解釈2 / 文章5 / 対話メタ1)。
    kind="text"プローブ=生成コードの実行ではなく出力テキストの決定論分類。
    分類器は合成47ケースで単体テスト(踏んだ罠: "①".isdigit()==True、ました/ませんの敬語判定漏れ)。
(b) `spec_holes --kind json`: 抽出・整形タスク対応。入力テキスト(文字列配列)に同じ指示をK回
    実行し、JSON出力のフィールド単位で発散検出。パース失敗率が実装不能率の代役。
    find_json は llm_client.py に共通化(default_probe と共用)。

### Qwen3.6-27B ioプロファイル(N=5適応, 18点, profile_*_io.json)
- 揺れる(要明示): txt_clarify 0.53(逆質問7 vs 一般論回答8)= 情報不足時の挙動は真の五分五分。
  ただし「架空データで進める」は0 = 捏造はしない。
- 安定だが意図とズレやすい罠:
  - io_range_count: 「3〜5個」→中央値4を発明(0.8)。下限でも上限でもない
  - io_pure_json: 「JSONで出力」→常に```フェンス付き(1.0)。パイプ直結は壊れる
  - io_key_style=camelCase(1.0) / io_date_ambig=MM/DD解釈(0.86) / txt_name_order=姓-名(1.0)
  - txt_sum_lang: 英語指示+日本語本文→日本語で応答(1.0)。出力言語は明示必須
  - txt_units: 単位は換算しない(ポンドのまま, 1.0) / io_missing_field=null埋め(1.0, 捏造せず)
- 実装不能: 0件。フォーマット追従はコードバッテリーより堅い(パース失敗0/15)。

### 抽出モードの実証(問い合わせメール3通 × K=5)
- [発散] quantity の型が不安定: "3〜5個"(文字列)×4 vs "3〜5"×1 / 10(数値)×4 vs "10"×1
- [合意]だが意図と逆: name=宛名「山田」を5/5全実行一致で選択(意図は差出人=佐藤)
  = 「安定だが意図とズレる」がbespokeタスクで実際に発生。[合意]リストの照合は必須
- date は正規化せず和文のまま("3月5日","来週の金曜日")。固定バッテリーio_date_fmtはISO既定
  だった=**文脈で既定が変わる**実例 → タスク駆動プローブ(spec_holes)の存在意義
- スキル構成の判断: 判断手順(2条件ルール・5ブロック・独立検証)はコードと共通のため
  分割せず weak-llm-playbook にステップ1.7として統合。

---
## 実測7: ioバッテリーのモデル間diff — Qwen vs Phi(2026-07-10)
配置: GPU1にQwen(QWEN_MEMUTIL=0.62)とPhi(:8002, util0.30)を同居。踏んだ罠:
util 0.62 のQwen起動が `max_num_seqs(1024) > Mambaキャッシュブロック(281)` で死ぬ
(Qwen3.6=GDNハイブリッド特有。デコード1系列=1 Mambaブロック)→ 起動スクリプトに
QWEN_MAXSEQS 環境変数を追加し 64 で解決(backup: start_qwen36_nvfp4.sh.bak.20260710)。

### 差分 6/18点(コード9/32点と同水準の差分率)
- **io_missing_field(最重要)**: Qwen=null埋め(1.0) vs **Phi=値を発明(0.73)+JSON不成立27%**。
  Phiに抽出を委譲して欠損の扱いを書き忘れると、静かにデータが捏造される。
- io_range_count: Qwen=中央値4を発明 vs Phi=下限3。**どちらも安定**なので乗り換え時に
  抽出値が黙って変わる(diffなしでは気づけない)
- io_pure_json: フェンス付き vs 説明文つき(両方「生JSONでない」が壊れ方が違う=後処理が違う)
- io_csv_header: ヘッダあり(1.0) vs 五分五分(0.53) / txt_name_order: 姓-名(1.0) vs 不安定(0.4)
- txt_clarify: 逆質問寄り五分五分 vs 一般論回答寄り(0.67)+捏造1回(Qwenは捏造0)
### 一致 12/18点: PhiもJA応答・敬語・散文要約・番号リスト等は同じ既定
### 結論
- コードバッテリーの知見がioでも再現: (1)モデル差は判断点の1/3規模で存在し--diffで機械抽出可能
  (2)弱いモデルほど「既定が違う」に加えて「不安定(揺れ・形式崩れ・捏造)」が増える
- 実務含意: 抽出・整形の委譲でモデルを乗り換えるなら、欠損値・範囲解釈・出力包装の3点は
  ほぼ確実に書き換え対象。

---
## 改善8: 測定系の汎用化 — パック/ドリフト検知/行列diff(2026-07-10)
(a) `--probes pack.json`: 判断点を外部JSONで宣言的に定義。ルールエンジン
    (_rule_match: regex/contains/len/json_parses/json_type/json_only_keys/json_field+
    equals・value_regex・is_null・absent・type/all/any、codeは cases: args→result|exception)。
    エラー系ラベルは"ERR"始まり規約で実装不能カテゴリに集計。単体テスト60項目全通過。
(b) `--assert baseline.json`: ドリフト検知(CI用)。★=既定変化(両側安定0.8+)・安定性低下・
    実装不能化→exit 1。△=不安定圏の多数派変化(警告のみ)。ライブ検証: 同一モデル→exit 0、
    Phiベースライン→io_range_count既定変化を検出しexit 1。
    ※検証時の教訓: パイプ越しに $? を見るとtailの終了コードを拾う(PIPESTATUS必須)。
(c) `--diff` をN個対応の行列比較に一般化(欠測は—表示)。
(d) packs/io_en.json: ioバッテリー英語版16点(tone/name_orderはJA固有のため除外)。
    idはJA版と共通、ラベルは英語(EN利用者はENプロファイル同士でdiffする想定)。

### 副産物の発見: 既定はプロンプト言語に依存する(Qwen3.6, JA io vs EN pack)
- io_date_fmt: JA=ISO安定(0.86) / EN=「原文のまま(March 5, 2026)」とISOの五分五分(0.53)
- io_range_count: JA=中央値4(0.8) / EN=中央値vs範囲保持の五分五分(0.53)
- 不変だった点: null埋め・camelCase・数値・bool・フェンス・指定キーのみ・MM/DD・ヘッダあり・
  日本語文には日本語応答・番号リスト・単位無換算 / txt_clarifyは両言語で五分五分(頑健な発見)
- 実務含意: **プロファイルは委譲で使う言語で測る**。他言語プロンプト流用時は要再測定。

---
## 改善9: スコープ拡張 — 指示遵守メタ/SQL/コスト計測(2026-07-10)
(a) packs/inst_ja.json(9点): 5ブロックテンプレの前提(出力契約・禁止・矛盾指示・位置感度・
    数量/字数制限が効くか)を測る。既存パック機構に乗るだけで本体変更なし。
(b) packs/sql_ja.json(7点, 新kind="sql"): 生成SQLをsqlite(:memory:)で実行し結果行で
    宣言的分類(rows/col0/row_count)。方言前提や構文崩れは ERR:sql→実装不能に集計。
(c) コスト軸: llm_client が usage を記録し、プロファイル行に avg_out_toks/avg_sec を保存。
    単体テスト36項目+既存60項目 全通過。

### 実測(Qwen3.6 vs Phi-3.5, N=5適応)
指示遵守メタ:
- Qwen: 字数制限(50字)だけ五分五分(0.53)、他8点は全て遵守1.0。
  → 出力契約・禁止・「後の指示が勝つ」・位置無関係は信頼してよい。字数だけ守れない。
- Phi: **否定形の遵守が0.6(40%でset禁止を破る)**=「弱いモデルは否定を無視しがち」の実証。
  **言語制約(英語で書け)は位置に関係なく完全無視(1.0)**。字数も超過0.86。
  ただし契約(2のみ)・矛盾後勝ち・禁止語・項目数・説明禁止は Phi でも効く。
- コスト軸が即機能: 同じ9点で Phi出力7638tok vs Qwen2295tok(3.3倍冗長)。
SQL:
- Qwen: 全7点安定1.0・エラー0。既定の罠: alice照合=大小区別で0件 / JOIN=INNERで
  部署なし従業員が消える / 購入者一覧=勝手にDISTINCT / NULL先頭 / LIMITで同点切り捨て。
- Phi: sql_empty_agg と sql_order_unspec が ERR:sql(実装不能)。dup_output=重複そのまま
  (Qwenと逆)。= 弱いモデルはSQLでも「書けない点」が増え、既定も入れ替わる。
### 結論
- 指示遵守はモデル固有の「効く契約/効かない契約」がある。プロファイルで契約の効力を
  事前確認してからテンプレを書くのが正しい順序(Qwenなら字数指定を諦めて文数指定にする等)。
- SQL委譲は case/join/distinct の3点をほぼ必ず急所ブロックに書く。

---
## 改善10: 英語対応の完成 — 全バッテリーの日英ペア化(2026-07-10)
(a) パック機構に "builtin" 参照を追加: 組み込み判断点の分類ロジックを共有したまま
    プロンプトだけ差し替え(+ "label_map" でラベル翻訳、"code_suffix" で添え書き差し替え)。
    → 言語ポートのコストが激減(code_en は31点をJSONだけで移植)。
(b) 新パック: code_en(31)/ inst_en(9)/ sql_en(7)。io_en(16)と合わせEN計63点。
    inst_en の言語制約プローブは「日本語で書け」に反転(決定論検出を確保)。
    単体テスト: EN機構24項目+回帰143項目 全通過。

### 発見: 指示遵守そのものが言語依存(弱いモデルほど強く効く)
- **Phi(JA→EN)で失敗が全消滅**: 否定形40%破り→遵守1.0 / 言語制約完全無視→遵守1.0 /
  SQL実装不能2点→両方正常 / SQL照合が大小区別→大小無視に反転 / DISTINCT既定も反転。
  = Phiの「指示を守れない」はモデルの性質ではなく**プロンプト言語の性質**だった。
- **Qwen(JA→EN, code31点)**: JAで揺れていた fib_index/clamp_pct/dup_count がENで全て安定1.0、
  avg_empty は既定が反転(JA=例外五分五分→EN=0返し0.8)。残り27点は言語間で一致。
  = 強めのモデルは頑健だが、それでも「揺れ」は言語で消えたり生まれたりする。
- Qwenの字数制限はENでも守れない(全实行が110-200字に超過)= 言語によらない弱点も分離できた。
- 実務含意: **委譲言語ごとにプロファイル必須**。英語ベンチの好成績は他言語委譲を保証しない。
  日本語で弱いモデルに委譲するなら、JAプロファイルの実装不能・不安定はEN測定から予測できない。

---
## 改善11: テスト同梱+急所ブロック自動生成+並列化(2026-07-10)
(a) 公開repoに tests/ を新設(それまで単体テストは私のtmpにしか無かった)。分類器・ルール
    エンジン・builtin機構の約170項目+全パックの --validate を tests/run_all.py が回し、
    GitHub Actions(py3.10/3.12)をCIとして追加。パックに probes[].tests(セルフテスト)を
    埋め込める仕組みも追加=コミュニティパックの品質担保が仕組み化。
(b) spec_holes: [発散]があるとレポート末尾に**急所ブロック案**を自動生成(候補挙動ごとに
    「f(入力) は X を返す」の貼るだけ1行+実装数)。発注側の急所執筆が選択作業に縮む。
    交互応答モック(:18998, mock_alt.py)でcode/json両モードの出力を決定論検証。
(c) default_probe --parallel N: スレッドローカルLLMClientでプローブ並列実行。
    実測: 6プローブ×N3 が 12.3s→3.9s(3.1倍)。適応リサンプリングは従来どおりプローブ内直列。
- 教訓再々確認: pkill -f はパターンをブラケット化しても、**シェルの同一コマンド行に
  平文のファイル名があれば自滅する**(mock起動とpkillを同じコマンドに書いた)。kill $PID が正。

---
## 改善12: モデルカード(委譲ガイド)生成器(2026-07-11)
`model_card.py`(新規): プロファイルJSON群→Markdownの委譲ガイドを自動生成。
- サマリ表(バッテリー×実装不能/揺れる/安定+出力tok・秒)/ 実装不能リスト /
  必ず明示リスト / 安定既定の照合チェックリスト / 生成元の来歴
- `--glob 'profile_Qwen*.json' -o card.md`。複数モデル混在ならモデルごとに連結。_partial自動除外。
- import安全(main()ガード)にして単体テスト化(tests/test_model_card.py、合成+同梱プロファイル)。
- repo cards/ に Qwen3.6-27B と Phi-3.5-mini の実カードを同梱。カードのサマリ表だけで
  言語効果が読める(Qwen: code JA=揺れ4 vs code EN=揺れ0 / Phi: sql JA=実装不能2 vs EN=0)。
- スキルの②照合は手書きの要注意リストではなくカードの「安定な既定」節と突き合わせる運用に変更。
※環境待ちは継続: GLMフルプロファイル(:8001停止中)/ Haikuクラウドdiff(ANTHROPIC_API_KEY未設定)。
